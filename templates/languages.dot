digraph languages {

  fontname="Helvetica,Arial,sans-serif";
  node[fontname="Helvetica,Arial,sans-serif"];
  edge[fontname="Helvetica,Arial,sans-serif"];
  node[color="lightblue2",shape="box",style="filled,rounded"];
  newrank=true;
  rankdir=LR;
  ranksep=10;


  {% for decade in decades %}
    subgraph cluster{{ decade.idx }} {
      label="{{ decade.label }}";
      rank=same;
      {% for language in decade.languages %}
        "{{ language }}";
      {% endfor %}
    }
  {% endfor %}

  {% for decade in decades %}
    {% for language in decade.languages_predecessors %}
      {% if language.predecessor %}
        "{{ language.predecessor }}" -> "{{ language.name }}";
      {% endif %}
    {% endfor %}
  {% endfor %}

}
